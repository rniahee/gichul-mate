# 프로젝트 규칙 (Claude Code용)

## 프로젝트 개요

프로젝트명: 기출메이트 (GichulMate) / 리포지토리명: `gichul-mate`
정보처리기사 필기 학습 서비스. 상세 스펙은 `docs/spec.md` (개발 구현 문서)를 항상 먼저 참고할 것.
기술 스택: Next.js 16 (App Router), TypeScript, Tailwind CSS, TanStack Query, Zustand,
PostgreSQL + Prisma, Gemini API, Recharts, Vitest.

작업을 시작하기 전 반드시 `docs/spec.md`를 읽고, 그 문서의 기능 정의·데이터 모델·로드맵과
어긋나지 않게 작업할 것. 문서 내용과 다르게 구현해야 할 이유가 있다면, 마음대로 바꾸지 말고
먼저 이유를 설명하고 사용자 승인을 받을 것.

## 작업 진행 방식 (반드시 지킬 것)

### A. 환경설정 단계 (초기 세팅 중에만 적용)

- 전체 설정을 한 번에 진행하지 말고, 항상 한 단계씩만 진행한다.
- 각 단계마다: (1) 이번 단계에서 무엇을, 왜 하는지 설명 → (2) 사용자가 터미널에 직접 입력할
  명령어를 제시 → (3) 명령을 대신 실행하지 않고, 사용자가 실행 결과를 알려줄 때까지 기다린다.
- 사용자가 결과(성공/에러 메시지)를 알려주면, 그 단계에 해당하는 커밋 메시지를 제안한다
  (형식은 D. 커밋 메시지 규칙 참고). 커밋 명령도 다른 명령과 동일하게 제안만 하고, 사용자가
  직접 실행하도록 안내한다.
- 커밋까지 끝났다고 사용자가 알려주면 그때 다음 단계로 넘어간다.
- 이 단계에서는 비즈니스 로직 코드를 작성하지 않는다.

### B. 기능 개발 단계 (환경설정 이후 모든 작업에 적용)

모든 기능/작업은 예외 없이 아래 6단계를 순서대로 따른다. 단계를 건너뛰거나 합치지 않는다.

1. **계획**: 구현할 작업을 파일 단위로 쪼갠 계획을 제시한다. 새로 만들 파일, 수정할 파일,
   API/DB 쿼리 설계, 예상되는 리스크를 포함한다.
2. **승인 대기**: 계획만 보여주고 절대 코드를 작성하지 않는다. 사용자가 "승인" 또는
   "진행해"라고 답하기 전까지 기다린다. 승인 전에는 파일을 생성/수정하지 않는다.
3. **코드 작성**: 승인 후에만 구현한다. 한 번에 너무 많은 파일을 만들지 않고, 리뷰 가능한
   단위로 나눠서 작업한다.
4. **테스트**: 구현 후 어떻게 확인해야 하는지 알려준다(실행 명령어, 확인할 화면/동작).
   자동으로 돌릴 수 있는 테스트(Vitest 등)는 직접 실행하고 결과를 보여준다.
5. **커밋 제안**: 테스트를 통과하면, 이번 작업 단위에 대한 커밋 메시지를 제안한다
   (형식은 D. 커밋 메시지 규칙 참고). 사용자가 승인하기 전까지 커밋을 실행하지 않는다.
   승인되면 커밋까지 실행하고 결과(커밋 해시 등)를 알려준다.
6. **다음 단계 승인 대기**: 사용자가 커밋 결과를 확인하고 다음으로 넘어가라고 하기 전까지
   새로운 작업을 먼저 시작하지 않는다.

### C. 공통 규칙

- 새로운 패키지/의존성을 추가해야 하면, 추가하기 전에 이유를 설명하고 승인을 받는다.
- `docs/spec.md`의 로드맵(9장) 순서를 기본 작업 순서로 따르되, 사용자가 다른 순서를
  요청하면 그에 따른다.
- 확실하지 않은 부분은 추측해서 진행하지 말고 먼저 질문한다.
- 파일이 삭제되거나 덮어써질 수 있는 명령(디렉토리 초기화, 강제 삭제, DB 리셋 등)은
  절대 그냥 실행하지 말고, 실행 전에 항상 먼저 경고하고 승인을 받는다.

### D. 커밋 메시지 규칙

- Conventional Commits 스타일을 따른다: `type: 한글 설명`
- 주로 쓰는 type
  - `chore`: 환경설정, 설정 파일, 패키지 설치
  - `feat`: 기능 추가
  - `fix`: 버그 수정
  - `docs`: 문서 수정 (spec.md, README 등)
  - `test`: 테스트 코드 추가/수정
  - `refactor`: 동작 변화 없는 코드 개선
- 설명은 간결하게 한글로 작성한다. 예: `chore: Prisma 초기화 및 초기 스키마 정의`,
  `feat: 문제 은행 조회 API 및 목록 페이지 구현`
- 커밋 단위 기준
  - 환경설정 단계: A의 단계 하나당 커밋 하나
  - 기능 개발 단계: `docs/spec.md` 로드맵의 한 단계를 기본 단위로 하되, 변경 범위가 크면
    의미 있는 하위 단위(예: API 구현 / 화면 구현)로 나눠 여러 커밋으로 제안해도 된다.
- 커밋 메시지를 제안할 때는 `git status`/`git diff` 요약도 함께 보여줘서, 무엇이
  커밋되는지 사용자가 확인하고 승인할 수 있게 한다.
